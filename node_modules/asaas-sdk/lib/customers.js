const GenericResource = require("./generic-resource");

class Customers extends GenericResource {
    constructor(config){
        super(config);

        this.resource = '/customers';
    }
}

module.exports = Customers;