const GenericResource = require("./generic-resource");

class Payments extends GenericResource {
  constructor(config) {
    super(config);

    this.resource = "/payments";
  }
}

module.exports = Payments;
